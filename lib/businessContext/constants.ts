// Shared constants for business context — no server-only, safe on both server and client.
// ~2,000 tokens ≈ 8,000 chars (conservative for mixed Vietnamese/English content).
export const MAX_BUSINESS_CONTEXT_CHARS = 8_000
